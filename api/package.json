{
  "name": "noun-gender-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create noun-gender-db",
    "d1:migrate": "wrangler d1 execute noun-gender-db --local --file=../data/schema.sql",
    "d1:migrate:remote": "wrangler d1 execute noun-gender-db --remote --file=../data/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.93.0"
  }
}
